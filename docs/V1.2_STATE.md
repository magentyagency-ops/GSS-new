# V1.2 — État de départ

> Date : 2026-06-15. Branche : `feat/v1.2-remove-decorative-blocks` (depuis `feat/no-template`).

## PR #5 (V1.1)
- **État : OPEN (non mergée)** au démarrage de V1.2.
- Conséquence : `feat/no-template` est à l'état **PR#4** (refonte V1) **sans** le fix V1.1
  (suppression `image8.jpeg` + `forceTextColor` épargnant les `txbxContent`).
- Décision (conforme au brief) : V1.2 part de `feat/no-template` et traite le problème **à la
  racine et de façon catégorielle** — il re-couvre donc et dépasse le périmètre V1.1.

## Base `feat/no-template` (HEAD = 96c4d18, merge PR #4)
Contient la refonte V1 : fond gris `#E5E5E5`, `stripStandaloneBgImages` (retire les images de
fond pleine page des pages dupliquées), `forceTextColor` (force le texte en `#1A1A1A` sur les
pages clonées — **y compris** les bandeaux de titre, d'où le bug de lisibilité).

## Objectifs V1.2
1. Supprimer **toutes** les images décoratives sous les bandeaux de section (catégoriel, pas image par image).
2. Rendre le **bandeau header de section** cohérent et lisible sur **chaque** page.

Périmètre élargi : `cloneSpread` modifiable. Verrouillés : fond gris `#E5E5E5`, Mode B (no-template).
